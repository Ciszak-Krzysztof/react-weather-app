const DEFAULT_ENDPOINTS = {
    DATA: "data/2.5",
};

export const API = {
  WEATHER: {
    CURRENT_WEATHER: `${DEFAULT_ENDPOINTS.DATA}/weather`,
    FORECAST: `${DEFAULT_ENDPOINTS.DATA}/forecast`,
  },
};
